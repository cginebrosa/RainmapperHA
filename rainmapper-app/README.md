# Rainmapper

Add-on de Home Assistant para ejecutar Rainmapper dentro de un contenedor.

Los datos persistentes se guardan en `/share/rainmapper` para que puedas acceder a ellos desde Home Assistant o desde una carpeta compartida.
